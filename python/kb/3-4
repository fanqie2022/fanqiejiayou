a = int(input("请输入a的值："))
b = int(input("请输入b的值："))
c = int(input("请输入c的值："))
if a>b:
    if a>c:
        max=a
    else:
        max=c
else:
    if b>c:
        max=b
    else:
        max=c
print ("max-", max)